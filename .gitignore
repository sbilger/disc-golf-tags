# scratch / verification artifacts
_check.js
_shot_*.png
*.tmp

# build scratch (keep generator scripts in build/, ignore intermediates)
build/shots/
build/frames/
build/video/
build/_*.png
build/doc.html
build/demo-night.csv

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# node (only if tooling is added later)
node_modules/
npm-debug.log*

# local secrets — never commit real Supabase keys
.env
.env.local
